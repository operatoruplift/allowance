# Preservation ledger — 20 September 2026

Inspection began at **2026-09-19T17:36Z** (20 September in Asia/Ho_Chi_Minh) on `main`, commit `6b397ada497216e34b0fee66efb6f9720bc0e4e2`. The working tree was clean. No applicable ancestor or repository `AGENTS.md` was present. The consolidated completion brief supplied for this release supersedes historical brief fragments; the user's later curved A identity remains the approved mark.

Preserved: React/Vite/TypeScript UI, Express single-operator service, SQLite journal/sessions, official pinned x402 v2 exact SVM stack, network-bound mainnet/devnet policies, existing MCP v2 stdio bridge, MIT license, the A14 red composition, local fonts/films, 26-composition brand kit and historical evidence. No second app, database provider, MCP framework, token or channel transport was introduced. Lotline and Buffer were not accessed or changed.

The baseline production deployment was the static no-spend rehearsal at `https://allowanceonsolana.vercel.app`, deployment `dpl_7PG5nFseWPo7XPgpFm3F1j6yix2D`. Its four hosted journeys and eleven asset hashes were verified in the preceding deployment task. Those checks remain dated baseline evidence; this release reruns its own checks. It has no persistent backend, signing or model runtime.

The baseline manifest pins Node `>=22.19.0 <25`, x402 `2.25.0`, Solana Kit `5.5.1`, split MCP server/client `2.0.0`, SQLite binding `13.0.3`, Argon2 `0.45.1` and OpenAI `7.15.0`. A clean lockfile install is part of the new verification. Existing implementation and validation claims are checked against source and newly recorded evidence rather than copied as current results.

Ownership for parallel work: durable runtime and recovery; payment/data/agent/MCP contracts; frontend and browser acceptance; root authentication, CI, configuration/evidence documentation and release. Independent reviews follow changes before deployment.

No real-USDC/test-token spending, paid model invocation, account funding, external message, registration or event submission is authorized by this implementation pass. Read-only public documentation/network checks and public deployment are authorized. Missing live credentials/funding are recorded separately from locally fixable gaps.
